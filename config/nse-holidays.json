{
  "_comment": "OPTIONAL and unused by capture. The daily job determines the trading session from the market data itself (see fetchMarketDate), so holidays need no configuration. This list only suppresses gap warnings for known closures and can safely stay empty.",
  "holidays": []
}
